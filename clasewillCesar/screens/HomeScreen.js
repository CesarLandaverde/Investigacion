import React from 'react';
import { View, Text, Button, StyleSheet } from 'react-native';

export default function HomeScreen({ navigation }) {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>Módulo: React Native</Text>
      <Text style={styles.description}>Este módulo cubre los fundamentos del desarrollo móvil con React Native y Expo.</Text>
      <Button title="Ver información del estudiante" onPress={() => navigation.navigate('Estudiante')} />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    padding: 20,
  },
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 10,
  },
  description: {
    fontSize: 16,
    marginBottom: 20,
  },
});
